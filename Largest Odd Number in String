 string largestOddNumber(string num) {
        int i=num.length()-1;
        for(  ; i>=0 ; i-- ){
            if(num[i]%2==1){
               break;
            }
        }
        string ans;
        for(int j=0; j<=i ; j++){
            ans+=num[j];
        }
        return ans;
  }
