 bool threeConsecutiveOdds(vector<int>& arr) {
        int low=0;
        int high=2;
        while(high<arr.size()){
            if(arr[low]%2!=0 && arr[low+1]%2!=0 && arr[high]%2!=0) return true; 
               low++;
               high++;

        }
               return false;
    }
